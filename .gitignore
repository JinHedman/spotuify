/target
**/*.rs.bk
Cargo.lock.bak

# Local secrets / caches that might land here during dev
.env
client.yml
.token_cache.json
*.log

# Local working notes — handover, structure, API reference.
# Deliberately untracked: the repo is public and these are internal.
notes/
